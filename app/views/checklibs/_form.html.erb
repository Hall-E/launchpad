<%= form_with(model: checklib, local: true) do |form| %>
  <% if checklib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(checklib.errors.count, "error") %> prohibited this checklib from being saved:</h2>

      <ul>
      <% checklib.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
